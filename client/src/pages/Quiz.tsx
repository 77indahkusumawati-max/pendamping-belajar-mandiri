// Signal & Focus: kuis singkat dengan umpan balik yang jelas dan langkah berikutnya.
import { useState } from "react";
import { ArrowLeft, ArrowRight, CheckCircle2, CircleHelp, RotateCcw, Trophy, XCircle } from "lucide-react";
import { Link } from "wouter";

const questions = [
  { question: "Apa bentuk umum dari fungsi kuadrat?", options: ["f(x) = ax + b", "f(x) = ax² + bx + c", "f(x) = a/x", "f(x) = a + b + c"], answer: 1, explanation: "Fungsi kuadrat memiliki pangkat tertinggi dua, sehingga bentuk umumnya adalah f(x) = ax² + bx + c." },
  { question: "Apa yang ditunjukkan oleh titik puncak grafik fungsi kuadrat?", options: ["Nilai maksimum atau minimum", "Jumlah akar saja", "Nama variabel", "Panjang sumbu-x"], answer: 0, explanation: "Titik puncak menunjukkan nilai maksimum atau minimum grafik, tergantung arah buka parabolanya." },
  { question: "Jika diskriminan bernilai nol, grafik memotong sumbu-x di...", options: ["Dua titik berbeda", "Tidak pernah", "Satu titik", "Tak terhingga titik"], answer: 2, explanation: "Diskriminan nol berarti persamaan memiliki satu akar kembar, sehingga grafik menyentuh sumbu-x di satu titik." },
];

export default function Quiz() {
  const [current, setCurrent] = useState(0);
  const [selected, setSelected] = useState<number | null>(null);
  const [score, setScore] = useState(0);
  const [finished, setFinished] = useState(false);
  const question = questions[current];

  const choose = (index: number) => { if (selected !== null) return; setSelected(index); if (index === question.answer) setScore((value) => value + 1); };
  const next = () => { if (current === questions.length - 1) { const finalScore = score + (selected === question.answer ? 0 : 0); localStorage.setItem("temanbelajar_quiz_score", String(finalScore)); setFinished(true); } else { setCurrent((value) => value + 1); setSelected(null); } };
  const reset = () => { setCurrent(0); setSelected(null); setScore(0); setFinished(false); };

  return <main className="min-h-screen bg-[#f6f1e8] px-5 py-6 text-[#1c2421] sm:px-8 lg:px-12 lg:py-8"><div className="mx-auto max-w-3xl"><Link href="/" className="inline-flex items-center gap-2 text-sm font-bold text-[#65716a] transition hover:text-[#e4694b]"><ArrowLeft size={16} /> Kembali ke ringkasan</Link>{finished ? <section className="mt-20 rounded-[28px] border border-[#1c2421]/10 bg-[#fbf8f3] p-8 text-center shadow-[0_12px_32px_rgba(28,36,33,.06)] sm:p-14"><span className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-[#f0dfc2] text-[#89683b]"><Trophy size={30} /></span><p className="mt-6 font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[#8a938d]">kuis selesai</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-.04em]">{score} dari {questions.length} benar</h1><p className="mx-auto mt-4 max-w-md text-sm leading-6 text-[#65716a]">Bagus. Lihat kembali pembahasan untuk memperkuat konsep yang masih terasa samar.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><button onClick={reset} className="inline-flex items-center gap-2 rounded-xl border border-[#1c2421]/15 px-4 py-3 text-sm font-bold transition hover:bg-[#f1ece3]"><RotateCcw size={16} /> Ulangi kuis</button><Link href="/" className="inline-flex items-center gap-2 rounded-xl bg-[#1c2421] px-4 py-3 text-sm font-bold text-[#f6f1e8] transition hover:bg-[#31443b]">Kembali belajar <ArrowRight size={16} /></Link></div></section> : <section className="mt-10"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[#e4694b]">evaluasi cepat · fungsi kuadrat</p><h1 className="mt-3 font-display text-4xl font-bold tracking-[-.04em]">Kuis hari ini</h1></div><span className="flex items-center gap-1.5 text-xs font-bold text-[#8a938d]"><CircleHelp size={16} /> {current + 1}/{questions.length}</span></div><div className="mt-6 h-1.5 overflow-hidden rounded-full bg-[#dce1db]"><div className="h-full rounded-full bg-[#e4694b] transition-all duration-300" style={{ width: `${((current + 1) / questions.length) * 100}%` }} /></div><div className="mt-8 rounded-[24px] border border-[#1c2421]/10 bg-[#fbf8f3] p-6 shadow-[0_12px_32px_rgba(28,36,33,.05)] sm:p-9"><p className="font-mono text-[10px] font-bold uppercase tracking-[.18em] text-[#8a938d]">pertanyaan {String(current + 1).padStart(2, "0")}</p><h2 className="mt-4 max-w-xl font-display text-2xl font-bold leading-tight">{question.question}</h2><div className="mt-7 space-y-3">{question.options.map((option, index) => { const isCorrect = selected !== null && index === question.answer; const isWrong = selected === index && index !== question.answer; return <button key={option} onClick={() => choose(index)} className={`flex w-full items-center gap-3 rounded-xl border px-4 py-3.5 text-left text-sm font-semibold transition active:scale-[.99] ${isCorrect ? "border-[#6e9978] bg-[#dce8d9] text-[#31483a]" : isWrong ? "border-[#e4694b] bg-[#f5dfd8] text-[#9f4635]" : "border-[#1c2421]/10 hover:border-[#e4694b]/50 hover:bg-[#f1ece3]"}`}><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-[#f0ece4] font-mono text-xs">{String.fromCharCode(65 + index)}</span><span className="flex-1">{option}</span>{isCorrect && <CheckCircle2 size={17} />}{isWrong && <XCircle size={17} />}</button>; })}</div>{selected !== null && <div className="mt-6 rounded-xl bg-[#eef3ec] p-4 text-sm leading-6 text-[#52715b]"><strong className="font-display">Pembahasan: </strong>{question.explanation}</div>}<button disabled={selected === null} onClick={next} className="mt-8 flex w-full items-center justify-center gap-2 rounded-xl bg-[#1c2421] py-3.5 text-sm font-bold text-[#f6f1e8] transition hover:bg-[#31443b] disabled:cursor-not-allowed disabled:opacity-40">{current === questions.length - 1 ? "Lihat hasil" : "Pertanyaan berikutnya"} <ArrowRight size={16} /></button></div></section>}</div></main>;
}
